os:
  - linux
  - windows
  - osx
language: node_js
node_js:
  - "4"
  - "6"
  - "8"
  - "10"
  - "12"
matrix:
  exclude:
    - os: windows
      node_js: "4"
before_install:
  - if [[ $TRAVIS_OS_NAME != 'windows' ]] && [[ `npm -v` == 2.* ]]; then nvm install-latest-npm; fi
